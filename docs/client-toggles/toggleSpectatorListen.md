## toggleSpectatorListen

## Description

Toggles the current player to listen to all players within their render distance, they will still only hear people within their proxmity.

```lua
exports['pma-voice']:toggleSpectatorListen()
```
